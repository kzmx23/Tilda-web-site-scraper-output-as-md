# Web Scraping Project

Production-ready веб-скрейпер с поддержкой Tilda CMS, аккордеонов и качественной очисткой контента.

## 🎯 Описание

Проект для качественного извлечения контента с веб-сайтов, с особым фокусом на сайты построенные на Tilda CMS. Включает автоматическую очистку от технического мусора, дедупликацию, извлечение скрытого контента из аккордеонов и сохранение структуры контента.

**Две версии скрейпера:**
- **v1** (`production_scraper.py`) - базовая версия для обычных сайтов
- **v2** (`production_scraper_v2.py`) - расширенная версия с поддержкой аккордеонов

## 📁 Структура Проекта

```
.
├── app/                          # Приложение скрейпера
│   ├── production_scraper.py     # v1 - базовый скрейпер
│   ├── production_scraper_v2.py  # v2 - с поддержкой аккордеонов
│   ├── scrapedo-web-scraper/     # Интеграция со Scrape.do
│   └── README.md                 # Документация по использованию
│
├── result/                       # Результаты скрейпинга
│   ├── site_a/                 # Результаты для site_a.com
│   │   ├── scraped_content_v2/   # Отдельные страницы (38 файлов)
│   │   ├── site_a_global_scraped_services.md  # Глобальный файл (142 KB)
│   │   ├── STRUCTURE_V2.md       # Детальная структура
│   │   └── README.md             # Описание результатов
│   │
│   └── site_b/                   # Результаты для site_b.ru
│       ├── scraped_content/      # Отдельные страницы (22 файла)
│       ├── site_b_global_products_services.md  # Глобальный файл (382 KB)
│       ├── scraping_summary.json # Статистика скрейпинга
│       └── README.md             # Описание результатов
│
├── venv/                         # Виртуальное окружение Python
├── CLAUDE.md                     # Best practices и гайды
├── create_site_b_global.py       # Скрипт создания глобального файла
└── README.md                     # Этот файл
```

## 🚀 Быстрый Старт

### 1. Установка

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install requests beautifulsoup4
```

### 2. Настройка (опционально)

Для обхода блокировок через Scrape.do:

```bash
# Создать файл с токеном
mkdir -p app/scrapedo-web-scraper/config
echo "ваш-токен" > app/scrapedo-web-scraper/config/token.txt
```

### 3. Использование

**Для обычных сайтов (v1):**
```bash
cd app

# Скрейпить одну страницу (тест)
python3 production_scraper.py

# Массовое сканирование
python3 production_scraper.py all
```

**Для сайтов с аккордеонами (v2):**
```bash
cd app

# Скрейпить одну страницу (тест)
python3 production_scraper_v2.py

# Массовое сканирование
python3 production_scraper_v2.py all
```

## 📚 Документация

- **[app/README.md](app/README.md)** - полная документация скрейпера
- **[result/site_a/README.md](result/site_a/README.md)** - описание результатов
- **[CLAUDE.md](CLAUDE.md)** - best practices для веб-скрейпинга

## ✨ Возможности

### Поддержка Tilda CMS
- Автоматическое определение Tilda-сайтов
- Корректное извлечение контента из специфичных классов
- Работа с распределенной структурой контента
- **v2: Извлечение скрытого контента из аккордеонов** (t585__accordion)

### Аккордеоны (v2)
- Автоматическое обнаружение аккордеонов по атрибуту `data-accordion`
- Извлечение заголовков и скрытого контента
- Маркировка заголовков аккордеонов символом ➕ в markdown
- Сохранение структуры "заголовок → содержимое"

### Очистка Контента
- Удаление технического мусора (JSON форм, служебных тегов)
- Фильтрация навигации, headers, footers
- Очистка от Tilda-специфичных маркеров
- **Защита Tilda-элементов** (не удаляются важные классы типа t585__header)

### Качество Данных
- Дедупликация повторяющегося контента
- Сохранение иерархии заголовков
- Структурированный markdown на выходе
- Rate limiting для безопасного скрейпинга

## 📊 Результаты

### Company A.com
**Статус:** Завершено ✅
**Скрейпер:** v1 (production_scraper.py)
**Страниц:** 38/45 успешно
**Размер:** 142 KB markdown
**Формат:** Структурированные MD файлы + глобальный файл (2,271 строка)

См. детали в [result/site_a/README.md](result/site_a/README.md)

### Company B.ru
**Статус:** Завершено ✅
**Скрейпер:** v2 (production_scraper_v2.py)
**Страниц:** 22/24 успешно (2 страницы не существуют - 404)
**Размер:** 382 KB markdown
**Формат:** Структурированные MD файлы + глобальный файл (2,814 строк)
**Особенность:** Включает извлеченный контент из 9+ аккордеонов на каждой странице продукта

См. детали в [result/site_b/README.md](result/site_b/README.md)

## 🛠️ Технологии

- **Python 3** - основной язык
- **BeautifulSoup4** - парсинг HTML
- **Requests** - HTTP запросы
- **Scrape.do** - обход блокировок (опционально)
- **Regex** - фильтрация и очистка

## 📖 Best Practices

### Для Tilda CMS
1. Используйте `body` как main_content (не `main` или `article`)
2. Извлекайте контент из специфичных классов (`tn-atom`, `t-descr`, и т.д.)
3. Фильтруйте JSON данные форм (`{"lid":...}`)
4. Удаляйте nominify маркеры
5. **НЕ удаляйте элементы с классами `t\d+__` (например, `t585__header`)** - это важные Tilda-компоненты

### Для аккордеонов (v2)
1. Ищите элементы с атрибутом `data-accordion="true"`
2. Заголовок обычно в элементе с классом, содержащим `title`
3. Контент в элементе с классом, содержащим `content`, `text` или `descr`
4. Извлекайте аккордеоны **ДО** обычного контента для правильного порядка
5. Используйте lambda для поиска: `class_=lambda x: x and 'title' in str(x).lower()`

### Критические баги при работе с BeautifulSoup
1. **Tilda detection**: `class_=lambda x: x and ('t396' in x or any('tn-' in cls for cls in x))`
   - НЕ работает: `'tn-' in ' '.join(x)` (BeautifulSoup передает список напрямую)
2. **Защита элементов**: Проверяйте `re.match(r'^t\d+__', cls)` перед удалением

### Общие
1. Всегда используйте виртуальное окружение
2. Добавляйте delays между запросами (1.5+ сек)
3. Используйте точную дедупликацию (не similarity-based)
4. Логируйте метрики для отладки

Подробнее см. [CLAUDE.md](CLAUDE.md#web-scraping-best-practices)

## 🔧 Расширение

### Добавление Нового Сайта

1. Создайте `site_structure.json` со списком URL
2. Запустите скрейпер: `python3 production_scraper.py all`
3. Проверьте качество в выходной директории
4. При необходимости добавьте специфичные фильтры

### Кастомизация Фильтров

Отредактируйте `TECH_NOISE_PATTERNS` в `app/production_scraper.py`:

```python
TECH_NOISE_PATTERNS = [
    r'ваш-паттерн',
    # ...
]
```

## 🐛 Troubleshooting

Частые проблемы и решения описаны в:
- [app/README.md](app/README.md#troubleshooting)
- [CLAUDE.md](CLAUDE.md#common-issues)

## 📝 TODO

- [ ] Добавить поддержку других CMS (WordPress, Bitrix)
- [ ] Автоматическое построение site_structure.json (sitemap crawler)
- [ ] Экспорт в другие форматы (JSON, HTML)
- [ ] Веб-интерфейс для управления скрейпингом
- [ ] Мониторинг изменений контента

## 📄 Лицензия

MIT